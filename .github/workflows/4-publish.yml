name: Publish 

on:
  workflow_dispatch:
    inputs:
      npm-channel:
        description: 'Prisma CLI NPM Channel'
        required: true
      asset-name:
        description: 'Name of the .vsix file in the release'
        required: true
      tag-name: 
        description: 'The release version to fetch from.'
        required: true
      

jobs:
  marketplace:
    needs: [pre]
    name: Publish to marketplace
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - uses: dsaltares/fetch-gh-release-asset@master
        with:
          repo: "carmenberndt/philodendron"
          version: "tags/${{ github.event.inputs.tag-name }}"
          file: ${{ github.event.inputs.asset-name }}
          token: ${{ secrets.GH_TOKEN }}
      - name: publish vsix to marketplace
        run: vsce publish --pat ${{ secrets.PAT}} --packagePath ${{github.workspace}}/${{ github.event.inputs.asset-name }}
